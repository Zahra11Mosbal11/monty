#include "monty.h"
/**
 * is_stack - prints the top
 * @head: stack head
 * @line_cont: line_number
 * Return: no return
*/
void is_stack(stack_t **head, unsigned int line_cont)
{
	(void)head;
	(void)line_cont;
	bus.lifi = 0;
}
/**
 * is_queue - prints the top
 * @head: stack head
 * @line_cont: line_number
 * Return: no return
*/
void is_queue(stack_t **head, unsigned int line_cont)
{
	(void)head;
	(void)line_cont;
	bus.lifi = 1;
}
